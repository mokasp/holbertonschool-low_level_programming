#include "main.h"

/**
 * _islower - check if lower case
 *
 * @c: checks if lowercase
 *
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	if (_islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

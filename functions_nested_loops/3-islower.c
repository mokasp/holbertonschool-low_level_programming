#include "main.h"

/**
 * _islower - check if lower case
 *
 * @c: checks if lowercase
 *
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	if (_islower(c) == 0)
	{
		_putchar(47);
	}
	else
	{
		_putchar(46);
	}
	return (0);
}

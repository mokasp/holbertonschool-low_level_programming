#include "main.h"

/**
 * is_prime_number - find if number is prime
 * @n: number
 *
 * Return: Always 0 (Success)
 */

int is_prime_number(int n)
{
	int i;

	i = n / 2;

	if (n = i * i)
		return (1);
	else
		return (0);
}
